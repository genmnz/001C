{
  "name": "@joeee/engine-controller",
  "version": "0.0.0",
  "type": "module",
  "description": "The UI-agnostic boundary: an observable view-state store + a command API that orchestrates the headless engine in a Web Worker. UIs depend on this, never on the engine or the event data directly.",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./worker": "./src/worker/engine.worker.ts"
  },
  "dependencies": {
    "@joeee/cytometry-core": "workspace:*",
    "@joeee/fcs": "workspace:*"
  },
  "sideEffects": false
}
