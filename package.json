{
  "name": "joeee",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "joeee — browser-native, headless-first flow cytometry engine",
  "workspaces": [
    "packages/*",
    "server",
    "app-web",
    "app-react"
  ],
  "scripts": {
    "test": "bun test",
    "test:wasm": "cd packages/cytometry-wasm && cargo test",
    "build:wasm": "cd packages/cytometry-wasm && rustup target add wasm32-unknown-unknown && RUSTFLAGS=\"-C target-feature=+simd128\" cargo build --release --target wasm32-unknown-unknown",
    "golden": "python scripts/golden/gen_logicle_golden.py",
    "serve": "bun run server/serve.ts",
    "typecheck": "tsc -p tsconfig.base.json --noEmit"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.3",
    "@webgpu/types": "^0.1.70"
  }
}
