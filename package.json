{
  "name": "joeee",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "joeee — browser-native, headless-first flow cytometry engine",
  "workspaces": [
    "packages/*",
    "server",
    "app-web"
  ],
  "scripts": {
    "test": "bun test",
    "serve": "bun run server/serve.ts",
    "typecheck": "tsc -p tsconfig.base.json --noEmit"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.3"
  }
}
